#include <iostream>

using namespace std;

int main()
{

     int a,max=-INT_MAX,max2=-INT_MAX+11,max2=-INT_MAX+2, min=INT_MAX, min2=INT_MAX+1, min3=INT_MAX+2,i;
     double s=1,n=0;

   for(i=0;i<7;i++){
    cout<<"a="<< i <<". szam:";
        cin>>a;
      if(a>max) {
           max3=max2;
           max2=max;
           max=a;
        }else if()
        if (a<min){
          min2=min
          min=a;
        }

   }
    a= max;
   int b= min;

   while(a!=b) {
        if(a>b) {
           a=a-b;
        }
        else {
           b=b-a ;
        }
       }


cout <<"lnko" << a << endl;
cout <<"lkkt" << max*min/a << endl;
    return 0;

    return 0;
}
